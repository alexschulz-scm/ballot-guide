# Ballot Guide — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi~=0.128.0
uvicorn[standard]~=0.30.0

# Rate limiting
slowapi~=0.1.9

# Database
aiosqlite~=0.20.0

# Data validation / config
pydantic~=2.9.0
pydantic-settings~=2.6.0

# HTTP client (used by MCP source clients)
httpx~=0.28.0

# AI / LLM SDKs
google-genai~=1.73.1

# MCP protocol
mcp~=1.0.0

# PDF parsing (optional — legislation server)
pdfplumber~=0.11.0

# Testing (dev only)
pytest~=8.2.0
pytest-asyncio~=0.24.0
