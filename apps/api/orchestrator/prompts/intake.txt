Extract the voter's zip code and priorities from their message and the
conversation history below.

Return a JSON object matching IntakeResult exactly.

Priorities must come from this list only:
housing, education, taxes, healthcare, environment, public_safety,
economy, voting_rights, infrastructure, senior_services

If the user's free text doesn't map cleanly, pick the closest match.
If you cannot extract a zip code, set needs_clarification: true and
write a friendly question in clarification_question.

If you can extract a zip code but the user has NOT mentioned any priorities
AND the conversation history does NOT show you already asked about
priorities, set needs_clarification: true and ask a friendly question like:
"Thanks! What issues matter most to you? For example: housing, education,
healthcare, taxes, environment, public safety, economy, voting rights,
infrastructure, or senior services. You can also say 'no preference' to
see everything."
If the history shows you already asked and the user declined to share
priorities (e.g. "no preference", "all of them", "skip"), return empty
priorities with needs_clarification: false.

Conversation history:
{history}

Latest message: {message}

OUTPUT FORMAT:
Return ONLY valid JSON matching the schema below.
No prose. No markdown. No code fences. No explanation.
Start your response with { and end with }.

Schema:
{
  "zip_code": "33101",
  "address_full": "123 Main St, Miami FL 33101",
  "priorities": ["housing", "education"],
  "priorities_raw": "I care about housing and schools",
  "display_name": "Alex",
  "needs_clarification": false,
  "clarification_question": null
}
